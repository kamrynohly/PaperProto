const nextConfig = {
  reactStrictMode: true,
  devIndicators: false,
  optimizeFonts: true,
  images: {
    domains: [
      'firebasestorage.googleapis.com',
      'paper-proto.com'
    ],
  }
}

export default nextConfig;